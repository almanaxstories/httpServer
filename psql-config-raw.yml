version: '3.1'

services:

  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: qwert
      POSTGRES_DB: usersInfoDB
      ports: 0.0.0.0:5432